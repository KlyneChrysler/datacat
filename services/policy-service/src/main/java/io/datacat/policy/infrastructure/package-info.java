/**
 * Outbound adapters: JPA entities and repositories, Kafka publishers. JPA
 * annotations live here and are mapped to/from domain records by dedicated
 * mappers — never in {@code domain}.
 */
package io.datacat.policy.infrastructure;
