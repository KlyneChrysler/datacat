server:
  port: ${PORT:8080}

spring:
  application:
    name: policy-service

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
