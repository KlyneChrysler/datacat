# Copy to terraform.tfvars (git-ignored) and fill in. Real values never
# enter the repo.
monthly_budget_usd  = 20
budget_email        = "you@example.com"
archive_bucket_name = "yourname-datacat-archive-staging"
